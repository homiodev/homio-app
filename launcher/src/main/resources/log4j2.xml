<?xml version="1.0" encoding="UTF-8" ?>

<Configuration monitorInterval="30" status="warn">
    <Properties>
        <Property name="logBaseDir">/opt/homio/logs</Property>
        <Property name="pattern">%d{yyyy/MM/dd HH:mm:ss} %-5p [%-25.25t] [%-25.25c{1}] - %m%n</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${pattern}"/>
        </Console>

        <RollingFile name="appLog"
                     fileName="${logBaseDir}/app.log"
          filePattern="${logBaseDir}/app-%d{yyyy-MM-dd}.log.gz"
          append="true">
            <PatternLayout pattern="${pattern}"/>
        </RollingFile>
    </Appenders>
    <Loggers>
        <logger name="org.homio" level="info">
            <AppenderRef ref="appLog"/>
            <AppenderRef ref="Console"/>
        </logger>

        <Root level="INFO">
            <AppenderRef ref="appLog"/>
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>
