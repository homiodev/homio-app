spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  banner:
    location: classpath:/banner.txt
  sql.init.mode: always
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    show-sql: true
